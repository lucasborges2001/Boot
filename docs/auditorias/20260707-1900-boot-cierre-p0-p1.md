# Auditoría Boot — cierre P0/P1 post hardening

## Contexto

Se aplicaron fases sucesivas para cerrar errores y warnings principales del auditor estructural.

## Validaciones reportadas

```bash
find . -name '*.php' -print0 | xargs -0 -n1 php -l
find . \( -name '*.sh' -o -path './scripts/test.sh' \) -print0 | xargs -0 -n1 bash -n
bash scripts/dev/smoke.sh
BOOT_REPORTS_DIR="$(mktemp -d)/reports" BOOT_SEND_TELEGRAM=false bin/boot-report --no-telegram --print | python3 -m json.tool >/dev/null
bash bin/boot-report-package
bash scripts/quality/audit_structure.sh ./submodules/Boot/
```

## Resultado del auditor

```txt
Structure audit v1.2.0
Root: /home/Lucas/dev/Pruebas/submodules/Boot
Profile: module
Summary: 0 error(s), 1 warning(s), 0 info
```

Warning restante:

```txt
FILE_TOO_LARGE :: lib/shell/collect.sh
```

## Cambios cerrados

| Área | Estado |
|---|---|
| Docs requeridos por perfil module | Cerrado. |
| `lib/render.sh` crítico | Cerrado. |
| `lib/system.sh` crítico | Cerrado. |
| Packaging reproducible | Cerrado y validado. |
| API method guards | Cerrado. |
| API JSON contract visible | Cerrado. |
| Headers/declare PHP relevantes | Cerrado. |
| Wrappers sospechosos SuperAdmin | Cerrado. |
| `base-resolver.php` require-after-code | Cerrado. |

## Decisión técnica

`lib/shell/collect.sh` no se refactorizó dentro de estas fases porque es runtime vigente de generación de `report.json`. Su extracción se deja como pendiente controlado con fixtures previos.

## Criterio de cierre

Boot queda aceptado como P0/P1 operativo. La siguiente mejora estructural debe abordar `collect.sh` con pruebas de equivalencia de JSON.
