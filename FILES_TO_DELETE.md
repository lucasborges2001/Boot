# Archivos a borrar

No hay archivos a borrar en esta fase.

Los wrappers `public_html/superadmin/support/config.php`, `paths.php`, `health.php` y `metrics.php` se conservan, pero dejan de ser archivos de `return` directo y pasan a exponer funciones de view model usadas por `_pageBootstrap.php`.
